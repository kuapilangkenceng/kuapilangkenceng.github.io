[
  {
    "description": "Tombol Hapus Layanan muncul untuk admin ATAU user dengan izin_hapus",
    "old": "      ${session.role === 'admin' ? `<button class=\"btn-simpan-status\" style=\"background:#c0392b;margin-top:6px\" onclick=\"hapusLayanan('${rec.id}')\">🗑️ Hapus Layanan</button>` : ''}",
    "new": "      ${(session.role === 'admin' || session.izin_hapus) ? `<button class=\"btn-simpan-status\" style=\"background:#c0392b;margin-top:6px\" onclick=\"hapusLayanan('${rec.id}')\">🗑️ Hapus Layanan</button>` : ''}"
  }
]